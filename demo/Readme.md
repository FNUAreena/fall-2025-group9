## 📊 Project Dashboard (User Interface)

This directory contains the source code for the interactive Streamlit Dashboard developed for the FCPS Meal Production Forecasting project.
This web application serves as the frontend interface, enabling users to visualize meal production trends, evaluate model results, and generate actionable insights

## 🔧 Prerequisites

Before running the dashboard, ensure the following steps are completed:

1. Run the Data Pipeline

The dashboard reads cleaned CSVs and model outputs generated by the backend.
Execute the full pipeline:

```bash
python src/maincode/main.py
```
2. Install UI Dependencies

Install Streamlit and all required libraries:
```bash
pip install -r ../requirements.txt
```
## 🚀 How to Run
To launch the web application locally:

Navigate to the project root directory.
Run the following command:
```bash
streamlit run demo/app.py
```

## Demo Folder

  ```
demo
├── fig
│   └── video.mp4
│
├── images
│   ├── multivariate_plots
│   │   ├── GRU.png
│   │   ├── LSTM.png
│   │   ├── fnn_model.png
│   │   ├── linear_regression.png
│   │   └── xgboost_model.png
│   │
│   └── univariate_plots
│       ├── GRU.png
│       ├── LSTM.png
│       ├── LSTM_train_test_forecast_example.png
│       ├── fnn_model.png
│       ├── linear_regression.png
│       └── xgboost_model.png
│
├── .gitkeep
└── app.py
```
